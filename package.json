{
  "name": "blackhole",
  "version": "0.1.0",
  "type": "module",
  "description": "A sample project using wasm-pack",
  "license": "MIT/Apache-2.0",
  "scripts": {
    "build": "wasm-pack build --target web --release",
    "serve": "http-server . -o -p 9999",
    "start": "npm run build && npm run serve"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
